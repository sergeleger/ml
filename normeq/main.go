package main

import "fmt"

func main() {

	fmt.Println(

		`The intention of this was to implement the normal equation: Θ = (X'*X)^-1 * X' * y. To calculate
the inverse of a matrix (A^-1) requires some more complex matrix operations:

- the determinant of a matrix
- the cofactor of a matrix

I'll try to implement at a later date.
`,
	)
}
